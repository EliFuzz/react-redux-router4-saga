import { pageName } from "Pages/Home/settings";

// counter
export const RESET = `RA\\${pageName}\\RESET`;
export const INCREMENT = `RA\\${pageName}\\INCREMENT`;
export const DECREMENT = `RA\\${pageName}\\DECREMENT`;
